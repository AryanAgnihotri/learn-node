 //node use CommonJS library, every node is module in node by default
 //module : encapsulated code only share the mininmum


 const names = require('./4-name');
 const sayHi = require('./5-utils');
 const data = require('./6-alternative-flavour');
 require('./7-mind-grenade');
//  console.log(names);
//  console.log(data);
//  sayHi('world');
//  sayHi(names.name1);
//  sayHi(names.name2);
//  sayHi(names.name3);
//  sayHi(secret);